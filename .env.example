# AWS Configuration (Optional - will auto-detect if not provided)
# The daemon will automatically detect AWS credentials in the following order:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. AWS shared credentials file (~/.aws/credentials)
# 3. AWS shared config file (~/.aws/config) 
# 4. IAM roles for tasks (ECS)
# 5. IAM roles for EC2 instances

# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# OR use IAM role instead of access keys
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/YourRole

# Region Configuration (optional)
# Include specific regions (comma-separated)
# REGIONS_INCLUDE=us-east-1,us-west-2,eu-west-1
# OR exclude specific regions (comma-separated)
# REGIONS_EXCLUDE=ap-southeast-1,ap-northeast-1

# ARN patterns to ignore during monitoring (comma-separated)
# Examples: arn:aws:s3:::*,arn:aws:logs:*,arn:aws:cloudwatch:*
ARN_IGNORE_PATTERNS=arn:aws:eks:::pod/*,arn:aws:rds:::snapshot/*

# Redis Configuration
REDIS_URI=redis://localhost:6379

# Monitoring Configuration
SLEEP_INTERVAL_SECONDS=300

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_TO_EMAILS=recipient1@example.com,recipient2@example.com
SMTP_USE_TLS=true
